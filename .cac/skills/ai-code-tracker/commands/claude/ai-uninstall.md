Uninstall the AI code tracker from this repository. This removes all git hooks, codeagent-cli hooks, opencode plugin, command files, and tracker config. Historical statistics in `.ai-tracking/authors/` are preserved.

Steps:
1. Run `node .cac/skills/ai-code-tracker/scripts/install.js --uninstall`
2. Report the results to the user
