Install the AI code tracker in this repository. Run the install script, then verify with a preflight check.

Steps:
1. Run `node .cac/skills/ai-code-tracker/scripts/install.js`
2. Run `node .cac/skills/ai-code-tracker/scripts/install.js --check`
3. Report the results to the user

If the install succeeded, remind the user to restart the current session so the project plugin loads.
