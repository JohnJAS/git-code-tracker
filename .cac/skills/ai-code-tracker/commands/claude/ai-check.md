Check whether the AI code tracker is installed and healthy.

Run `node .cac/skills/ai-code-tracker/scripts/install.js --check`

Report the results to the user. If there are issues, suggest running `/project:ai-install` or `/project:ai-repair`.
